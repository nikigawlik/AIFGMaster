{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "runserver",
            "type": "shell",
            "command": "java -Djava.library.path=lib/native -jar sawhian.jar",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}